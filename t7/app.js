// more about require
// JSON js object notation (collection of name - value pairs)
// require can fetch a folder

var greet = require('./greet');
//if there is no require it looks for greet.js native module or it looks for folder

greet.en();
greet.es();